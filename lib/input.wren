class Input {
    foreign static add_action(name)
    foreign static action_add_key(name, key)
    foreign static is_action_pressed(name)
    foreign static is_action_just_pressed(name)

    foreign static get_key_code(name)
}